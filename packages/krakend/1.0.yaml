image: krakend:latest
environment: {}
network:
  external:
    "@port@": "8080"
  internal: {}
volumes:
  config:
    mountpoint: /etc/krakend
questions:
  port:
    query: "What external port should KrakenD listen on?"
    type: port
notes:
  URL: "http://localhost:@port@"
