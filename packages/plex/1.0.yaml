image: plexinc/pms-docker
description: Media server for streaming movies, TV shows, and music
supplies: ["http"]
network:
  internal:
    "@port@": "32400"
volumes:
  media:
    mountpoint: /data
    quota: "@mediasize@"
  config:
    mountpoint: /config
questions:
  port:
    query: "What port should Plex listen on?"
    type: port
  mediasize:
    query: "How much storage should be allocated for media?"
    type: bytes
notes:
  url:
    value: "https://@PACKAGE_DNS@:@port@/web"
    type: url
