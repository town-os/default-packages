image: hitch:latest
environment: {}
network:
  external:
    "@port@": "443"
  internal: {}
volumes:
  config:
    mountpoint: /etc/hitch
questions:
  port:
    query: "What external port should Hitch listen on for TLS?"
    type: port
notes:
  URL: "https://localhost:@port@"
