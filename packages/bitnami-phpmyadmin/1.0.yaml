image: bitnami/phpmyadmin:latest
environment:
  DATABASE_HOST: "@dbhost@"
network:
  external:
    "@port@": "8080"
  internal: {}
volumes: {}
questions:
  dbhost:
    query: "What is the MySQL/MariaDB server hostname?"
    type: hostname
  port:
    query: "What external port should phpMyAdmin listen on?"
    type: port
notes:
  URL: "http://localhost:@port@"
