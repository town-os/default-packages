image: bitnami/haproxy:latest
description: Reliable high-performance TCP/HTTP load balancer (Bitnami)
supplies: ["http"]
network:
  external:
    "@port@": "8080"
volumes:
  config:
    mountpoint: /bitnami/haproxy/conf
questions:
  port:
    query: "What external port should HAProxy listen on?"
    type: port
notes:
  URL:
    value: "http://localhost:@port@"
    type: url
  Config:
    value: "Mount haproxy.cfg in the config volume"
