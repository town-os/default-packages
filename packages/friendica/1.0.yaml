image: friendica
description: Decentralized social networking platform
supplies: ["http"]
environment:
  FRIENDICA_URL: "http://@PACKAGE_DNS@:@port@"
  FRIENDICA_ADMIN_MAIL: "@adminmail@"
  MYSQL_HOST: "@dbhost@"
  MYSQL_USER: "@dbuser@"
  MYSQL_PASSWORD: "@dbpass@"
  MYSQL_DATABASE: "@dbname@"
network:
  internal:
    "@port@": "80"
volumes:
  html:
    mountpoint: /var/www/html
questions:
  adminmail:
    query: "What is the admin email address for Friendica?"
  dbhost:
    query: "What is the MySQL/MariaDB server hostname?"
    type: dns
  dbuser:
    query: "What is the MySQL database username?"
  dbpass:
    query: "What is the MySQL database password?"
  dbname:
    query: "What is the MySQL database name?"
  port:
    query: "What external port should Friendica listen on?"
    type: port
notes:
  URL:
    value: "http://@PACKAGE_DNS@:@port@"
    type: url
  Database:
    value: "Requires an external MySQL/MariaDB server"
