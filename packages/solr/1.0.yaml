image: solr:latest
environment: {}
network:
  external:
    "@port@": "8983"
  internal: {}
volumes:
  data:
    mountpoint: /var/solr
questions:
  port:
    query: "What external port should Solr listen on?"
    type: port
notes:
  URL: "http://localhost:@port@"
