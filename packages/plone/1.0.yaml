image: plone:latest
environment:
  SITE: "Plone"
network:
  external:
    "@port@": "8080"
  internal: {}
volumes:
  data:
    mountpoint: /data
questions:
  port:
    query: "What external port should Plone listen on?"
    type: port
notes:
  URL: "http://localhost:@port@"
