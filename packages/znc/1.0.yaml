image: znc:latest
environment: {}
network:
  external:
    "@port@": "6697"
  internal: {}
volumes:
  data:
    mountpoint: /znc-data
questions:
  port:
    query: "What external port should ZNC listen on?"
    type: port
notes:
  URL: "https://localhost:@port@"
  Setup: "Run 'docker run -it -v znc-data:/znc-data znc --makeconf' first to create initial configuration"
