image: znc:latest
description: Advanced IRC bouncer
network:
  external:
    "@port@": "6697"
volumes:
  data:
    mountpoint: /znc-data
questions:
  port:
    query: "What external port should ZNC listen on?"
    type: port
notes:
  URL:
    value: "ircs://localhost:@port@"
    type: url
