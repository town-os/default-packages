image: bitnami/nginx:latest
environment: {}
network:
  external:
    "@port@": "8080"
  internal: {}
volumes:
  app:
    mountpoint: /app
questions:
  port:
    query: "What external port should Nginx listen on?"
    type: port
notes:
  URL:
    value: "http://localhost:@port@"
    type: url
