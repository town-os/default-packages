image: registry
description: Docker container image registry
supplies: ["storage"]
network:
  external:
    "@port@": "5000"
volumes:
  data:
    mountpoint: /var/lib/registry
questions:
  port:
    query: "What external port should the Docker registry listen on?"
    type: port
notes:
  URL:
    value: "http://@PACKAGE_DNS@:@port@"
    type: url
  Push:
    value: "docker push @PACKAGE_DNS@:@port@/myimage:tag"
