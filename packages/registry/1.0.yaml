image: registry:latest
environment: {}
network:
  external:
    "@port@": "5000"
  internal: {}
volumes:
  data:
    mountpoint: /var/lib/registry
questions:
  port:
    query: "What external port should the Docker registry listen on?"
    type: port
notes:
  URL: "http://localhost:@port@"
  Push: "docker push localhost:@port@/myimage:tag"
