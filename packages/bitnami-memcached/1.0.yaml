image: bitnami/memcached
description: High-performance distributed memory caching system (Bitnami)
supplies: ["cache"]
network:
  external:
    "@port@": "11211"
questions:
  port:
    query: "What external port should Memcached listen on?"
    type: port
notes:
  Connection:
    value: "localhost:@port@"
