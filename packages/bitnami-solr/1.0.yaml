image: bitnami/solr:latest
description: Enterprise search platform (Bitnami)
supplies: ["search"]
network:
  external:
    "@port@": "8983"
volumes:
  data:
    mountpoint: /bitnami/solr
questions:
  port:
    query: "What external port should Solr listen on?"
    type: port
notes:
  URL:
    value: "http://localhost:@port@"
    type: url
