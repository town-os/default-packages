image: redis:latest
environment: {}
network:
  external:
    "@port@": "6379"
  internal: {}
volumes:
  data:
    mountpoint: /data
questions:
  port:
    query: "What external port should Redis listen on?"
    type: port
notes:
  Connection:
    value: "redis://localhost:@port@"
    type: url